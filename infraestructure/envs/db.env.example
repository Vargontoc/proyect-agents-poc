# Database environment variables
# Copy this file to db.env and fill in real values
# db.env is gitignored — never commit real values

POSTGRES_DB=appdb
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password