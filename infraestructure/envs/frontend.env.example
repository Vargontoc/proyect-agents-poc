# Frontend environment variables
# Copy this file to frontend.env and fill in real values
# frontend.env is gitignored — never commit real values

# API base URL consumed by Vue
VITE_API_BASE_URL=http://localhost:8080/api

# App config
VITE_APP_TITLE=Generic Base Project
VITE_APP_ENV=local